{
  "image": "mcr.microsoft.com/devcontainers/universal:2",

  "postCreateCommand": "if [ ! -d \"$HOME/google-cloud-sdk\" ]; then curl -fsSL https://sdk.cloud.google.com | bash -s -- --disable-prompts; fi && grep -qxF 'export PATH=\"$HOME/google-cloud-sdk/bin:$PATH\"' ~/.bashrc || echo 'export PATH=\"$HOME/google-cloud-sdk/bin:$PATH\"' >> ~/.bashrc"
}
